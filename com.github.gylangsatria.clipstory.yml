id: com.github.gylangsatria.clipstory
runtime: io.elementary.Platform
runtime-version: '8'
sdk: io.elementary.Sdk
command: com.github.gylangsatria.clipstory

finish-args:
  # Wayland with X11 fallback
  - --socket=wayland
  - --socket=fallback-x11

modules:
  - name: clipstory
    buildsystem: meson
    sources:
      - type: dir
        path: .